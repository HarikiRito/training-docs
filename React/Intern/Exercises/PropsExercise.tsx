import React from 'react';

/**
 * Exercise: Props and Component Composition
 * 
 * Task: Create a user management application with the following requirements:
 * 
 * Requirements:
 * 1. Define an interface for User with properties:
 *    - name (string)
 *    - email (string)
 *    - age (number)
 * 
 * 2. Create a UserProfile component that:
 *    - Receives user details as props
 *    - Displays user information in a structured format
 * 
 * 3. Create a UserList component that:
 *    - Maintains an array of users
 *    - Renders multiple UserProfile components
 *    - Uses map to dynamically render user profiles
 * 
 * Hints:
 * - Use TypeScript interface for type safety
 * - Leverage prop passing between components
 * - Use key prop when mapping over components
 * - Style components to improve readability
 */

// TODO: Define User interface here

// TODO: Create UserProfile component

// TODO: Create UserList component with user data and rendering logic

function PropsExercise() {
  // Placeholder for main component
  return <div>User Management Exercise</div>;
}

export default PropsExercise; 