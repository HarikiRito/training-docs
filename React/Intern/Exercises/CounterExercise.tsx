import React, { useState } from 'react';

/**
 * Exercise: Counter Component
 * 
 * Task: Create a functional component that implements a counter with the following features:
 * 
 * Requirements:
 * 1. Use useState hook to manage the counter state
 * 2. Implement an increment button that increases the counter
 * 3. Implement a decrement button that decreases the counter
 * 4. Ensure the counter never goes below 0
 * 5. Add a reset button that sets the counter back to 0
 * 
 * Hints:
 * - Use useState to create the state variable
 * - Create handler functions for increment, decrement, and reset
 * - Use Math.max() to prevent negative values
 * - Render the current count and buttons in the return statement
 */
function CounterExercise() {
  // TODO: Implement your counter logic here
  
  return (
    <div>
      {/* TODO: Render your counter UI */}
    </div>
  );
}

export default CounterExercise; 